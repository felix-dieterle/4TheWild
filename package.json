{
  "name": "4thewild",
  "version": "1.0.0",
  "description": "Silent Place Finder – find the quietest spots near you",
  "private": true,
  "scripts": {
    "prepare:android": "rm -rf www && mkdir www && cp index.html app.js style.css www/ && cp -r lib www/",
    "android": "npx cap run android",
    "build:android": "npm run prepare:android && npx cap sync android && cd android && ./gradlew assembleDebug",
    "open:android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "7.5.0",
    "@capacitor/core": "7.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "7.5.0"
  }
}
